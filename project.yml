targetNamespace: ""
parameters: {}
packages:
  - name: sample
    environment: {}
    parameters: {}
    annotations: {}
    actions:
      - name: hello
        binary: false
        main: ""
        runtime: "nodejs:default"
        web: true
        parameters: {}
        environment: {}
        annotations: {}
        limits: {}
environment:
  PG_HOST: ${PG_HOST}
  PG_DATABASE: ${PG_DATABASE}
  PG_PORT: ${PG_PORT}
  PG_USER: ${PG_USER}
  PG_PASSWORD: ${PG_PASSWORD}
